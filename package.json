{
	"name": "design-patterns",
	"version": "1.0.6",
	"description": "Collection of my studies and implementations of various design patterns",
	"main": "",
	"scripts": {
		"precommit": "npm version %npm_config_bump% --no-git-tag-version && git add package*.json",
		"commit": "git commit",
		"postcommit": "git push"
	},
	"dependencies": {
		"@types/node": "^20.11.27",
		"typescript": "^5.4.2"
	}
}
